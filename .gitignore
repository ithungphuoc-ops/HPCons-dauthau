node_modules/
build/
dist/
.next/
dist-desktop/
coverage/
.DS_Store
*.log
.env*
!.env.example
.vercel
next-env.d.ts
*.tsbuildinfo

# Bản backup demo cũ trước khi dọn dữ liệu — có mật khẩu plaintext, không đưa lên Git
src/data/staff_predemo_backup.json

# Dữ liệu dự án đấu thầu đang làm việc thật — Sếp muốn giữ lại, đẩy lên sau (2026-07-30)
src/data/db_predemo_backup.json

# Tệp Excel nghiệp vụ chị Trâm để tạm trong thư mục repo để Claude đối chiếu (góp ý, mẫu báo cáo
# ISO ~8MB). Là dữ liệu thật của Phòng + rất nặng, không đưa lên Git.
GopY_HPC*.xlsx
PHONG DAU THAU*.xlsx
# Tệp tạm Excel tự sinh khi đang mở file (~$Ten.xlsx)
~$*
