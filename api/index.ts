// Vercel Serverless Function — nhận mọi request /api/* (rewrite khai báo trong vercel.json)
// và giao cho Express app xử lý theo đúng route gốc.
import app from "../server.js";

export default app;
