# BÀN GIAO CÔNG VIỆC — 27–28/07/2026

Nhánh: `nangcap-chi-tram` · Cây làm việc **sạch** (đã commit hết) · `npm run lint` sạch.
Có **34 commit** trong 2 ngày (từ sau `54e4ae1`). File `.bundle` kèm theo chứa toàn bộ lịch sử nhánh.

> Push GitHub đang bị **403** (tài khoản `ksngotram14-collab` không có quyền ghi vào
> `ithungphuoc-ops/HPCons-dauthau`). Vì vậy dùng file `.bundle` để chuyển máy/acc:
> `git clone ban-giao-nangcap-chi-tram-2026-07-28.bundle`  (hoặc `git fetch <file>` vào repo sẵn có).
> Bản mới nhất: **`ban-giao-nangcap-chi-tram-2026-07-28.bundle`** (đã verify "complete history").

---

## ĐÃ LÀM XONG (đều đã test trên Bản thử, không lỗi console)

1. Chốt tiến độ Phòng 100% ở cửa 3 → 4, Bộ phận 100% ở cửa 2 → 3 — chặn cả Quản lý lẫn Trưởng
   phòng. Hằng số: `CHOT_TIEN_DO_PHONG_KHI_ROI_BUOC`, `CHOT_TIEN_DO_BO_PHAN_KHI_ROI_BUOC` trong `src/App.tsx`.
2. Duyệt đủ 100% ở cửa chặn là hồ sơ đi tiếp luôn — không phải kéo thẻ lại lần nữa.
3. **Kéo hồ sơ về bước trước (MỌI đường: 4→3, "kéo hẳn về Bước 1", luồng dời hạn, CĐT điều
   chỉnh): GIỮ NGUYÊN kết quả kiểm tra & tệp đính kèm**, chỉ reset tiến độ Phòng về 0%.
4. TP bấm "Lưu Hồ Sơ" ở Bước 1 là hồ sơ lên Bước 2.
5. Công việc con mới KHÔNG kế thừa "Đã trúng/Rớt thầu" của dự án cha.
6. Thông báo từ bước 3 trở đi KHÔNG báo Quản lý & Nhân viên, trừ TRÚNG/RỚT THẦU và kéo về Bước 1/2.
7. Quản lý đẩy hồ sơ sang bước 3 → báo chuông toàn bộ Trưởng phòng.
8. Khoá việc VÒNG MỚI chưa được TP duyệt ở màn nhân viên (`hoSoChoTPDuyet` xét cả `choDuyetLai`).
9. Dashboard Quản lý chỉ thống kê hồ sơ họ LÀM QUẢN LÝ (chính hoặc phụ).
10. Bản thử: thanh L1/L2/L3/L4 góc dưới trái, 4 level luôn bấm được ngay.
11. Sửa nhãn quyền Level 4 + chuẩn hoá văn phong thông báo trung tính.
12. Báo nhân viên khi Quản lý/Trưởng phòng sửa tay % tiến độ việc con của họ (tăng hay giảm đều báo).
13. **Hạn Phòng/hạn thầu so theo NGÀY** — trước so cả giờ nên báo trễ ngay từ 0h của chính ngày hết hạn.
14. **Bỏ Delay Log tự động** khi Quản lý chỉ sửa form bình thường (hạn lùi xa hơn) — Delay Log
    giờ chỉ ghi đúng ở luồng "kéo hồ sơ về Bước 1" (CĐT yêu cầu điều chỉnh).
15. **Sửa công thức tính "số ngày dời"** ở modal "Dời hạn & sửa việc con": tính theo TỔNG NGÀY việc
    con tăng thêm (không phải mốc lịch xa nhất) — trước đó thêm việc con chạy song song không đẩy
    hạn ra dù khối lượng việc rõ ràng tăng.
16. **Tự động đăng xuất khi bị đổi quyền/khoá tài khoản giữa phiên** — trước đây phải tự đăng
    xuất/đăng nhập lại mới nhận quyền mới; giờ phát hiện gần như ngay lập tức qua đồng bộ Firestore.
17. **Sửa dropdown "Quản lý phụ trách"** trong form nhân sự: người vừa được thăng cấp/đổi quyền
    khỏi Level 2 không còn bị hiển thị nhầm "Chưa gán" — vẫn hiện đúng tên, kèm ghi chú để Trưởng
    phòng biết mà gán lại người khác.
18. **Viết lại README.md / HUONG-DAN-CHO-IT.md / .env.example** theo đúng kiến trúc Next.js +
    Vercel + Firebase hiện tại (trước đó còn mô tả Vite/server.ts/Gemini API cũ, IT đọc theo sẽ sai).

### Bổ sung cuối ngày 28/07

19. **Mốc thời gian & hạn thầu tính theo VÒNG hiện tại** (cả App lẫn form) — hồ sơ sang vòng 2
    không còn kẹt mốc vòng 1. Hằng số/hàm: `getRoundStart`, `getExecEnd` dùng `tasksOfRound`.
20. **Xuất Excel thêm cột "Chi Tiết Theo Vòng (Số Lần Gửi CĐT)"** — mỗi vòng một dòng: khoảng
    thời gian, số ngày, ngày gửi CĐT, tiến độ vòng đó. Dùng để đo hiệu suất phòng.
21. **KPI để TRỐNG điểm ở mọi nơi** (đang xây dựng trọng số) — bỏ luôn xếp hạng theo điểm và màu
    huy chương để không ngụ ý thứ bậc. Dữ liệu `kpiDiem` vẫn giữ, chỉ ẩn hiển thị.
22. **Danh sách nhân sự không tính**: Khách mời (L4), Ban giám đốc, Quản trị hệ thống — gom vào
    hằng số `CHUC_VU_KHONG_TINH_NHAN_SU`. Vẫn giữ đủ ở tab "Đội ngũ" để quản lý tài khoản.
    Khách mời cũng bị loại khỏi ô chọn người giao việc.
23. **Bản thử sạch hồ sơ giả lập** + nút **"Nạp 3 dự án NHÁP"** (3 dự án cha + 3 công việc con)
    để nghiệm thu vòng xuất/khôi phục.
24. **Nút "Sao lưu" / "Khôi phục"** (chỉ TP) — tệp JSON giữ ĐỦ cây việc con, tiến độ, vòng, nhật ký
    gửi CĐT; nạp lại khôi phục nguyên trạng 100%. Không xuất mật khẩu. Đã test: sao lưu → xoá sạch
    → khôi phục khớp hoàn toàn.
25. **Fix đọc CSV không có BOM** ở `/api/projects/import` — trước đây tiếng Việt hỏng nên báo
    "thiếu cột bắt buộc" dù tệp đúng mẫu (Excel bản Việt lưu CSV mặc định không kèm BOM).
26. **Bấm thẻ Kanban/chuông/Dashboard tự bật đúng nút lọc** (Đang làm / Đã xong) rồi mới mở hồ sơ —
    trước đây hồ sơ đã xong nằm ngoài bộ lọc nên bấm vào không thấy gì.
27. **Siết quy trình kéo về Bước 1**: Quản lý bỏ lựa chọn "giữ nguyên hạn" (đã kéo về = phải phân
    bổ lại việc con); TP kéo thẳng rồi báo Quản lý phân bổ lại. **Cấm đẩy thẻ tiến lên khi hồ sơ
    đang chờ TP duyệt kế hoạch** — trước đây Quản lý kéo thẳng Bước 1 → 2 được, lách cả quy trình.

## CÒN TREO — CHỜ CHỊ TRÂM CHỐT (chưa đụng code)

- **Nâng/hạ quyền tài khoản** — 2 điểm còn lại nằm NGOÀI tầm app (không phải bug, là giới hạn
  kiến trúc SSO):
  1. Nâng quyền trong app này sẽ bị **App Tổng ghi đè** khi đăng nhập lại → phải nâng ở
     account.hpcore.vn (nguồn quyền chính thức).
  2. L3→L2: đội ngũ trống, phải gán lại nhân viên cho quản lý mới (là hành vi đúng, không phải lỗi).

## LƯU Ý TRƯỚC KHI GỬI IT

- `.env.local` **có khoá thật** (`HPCORE_FIREBASE_SERVICE_ACCOUNT`, `FIREBASE_ADMIN_PRIVATE_KEY`) —
  gửi nguyên folder là gửi luôn khoá. Tách ra gửi kênh riêng (xem `HUONG-DAN-CHO-IT.md` mục 8).
- `.env.local` đang để `NEXT_PUBLIC_DEV_SANDBOX=1` → IT chạy `npm run dev` sẽ vào màn "Bản thử".
  **Đổi về 0** trước khi bàn giao production — CHƯA đổi vì đang cần giữ Bản thử để tiếp tục nghiệm
  thu; đổi ngay trước lúc thật sự bàn giao, đừng đổi sớm giữa chừng.
- Nhánh `nangcap-chi-tram` vẫn CHƯA đẩy lên GitHub được (403). Toàn bộ 34 commit chỉ nằm trên máy
  này + file `.bundle` — nên đẩy lên hoặc sao lưu ra nơi khác sớm.

## MAI LÀM TIẾP — CHỖ ĐANG DỞ

- Chị Trâm báo "Quản lý kéo từ Bước 2 về Bước 1 chọn giữ nguyên hạn thì **không cho kéo**" —
  phần **không báo cho TP** đã sửa xong, nhưng phần "không cho kéo" **chưa tái hiện được**
  (trên máy thử kéo bình thường). Cần chị mô tả rõ: kéo bằng chuột hay bấm nút mũi tên ‹,
  và sau khi bấm thì thẻ đứng yên hay có báo lỗi gì hiện lên.
  → Lưu ý: lựa chọn "giữ nguyên hạn" nay đã BỎ khỏi hộp thoại của Quản lý (mục 27), nên có thể
  tình huống này không còn nữa.
